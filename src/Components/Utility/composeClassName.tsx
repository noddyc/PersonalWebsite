// import { ComponentType } from 'react';

// const composeClassName = (WrappedComponent: ComponentType, additionalClassName: string) => {
//   // eslint-disable-next-line
//   console.log(additionalClassName);
//   return <WrappedComponent className={additionalClassName} />;
// };

// export default composeClassName;
